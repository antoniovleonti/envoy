syntax = "proto3";

package envoy.extensions.resource_monitors.event_loop_latency.v3;

// The event loop latency resource monitor tracks the utilization (fraction of time spent ready to execute
// or actively executing callbacks) of the event loops (dispatchers).
message EventLoopLatencyConfig {
}
